jobs:
  include:
    - language: python
      python:
        # PyPy versions
        - "pypy3"  # currently Python 3.6.1,  PyPy 7.1.1-beta0
      # command to install dependencies
      install:
        - pip install pytest
      # command to run tests
      script:
        - cd tests
        - pytest
    - language: php
      php:
        - 7.4
      install:
        - sudo apt update
        - sudo apt install mysql-server
      script:
        - sudo mysql -u root < /Code/Database/srijas.sql
        - echo '{"server_name":"localhost","user_name":"root","password":"","db_name":"srijas"}'>Code/Web_app/parameters.json
        - cd Code/Web_app
        - php test.php
        
