jobs:
  include:
    - language: python
      python:
        # PyPy versions
        - "pypy3"  # currently Python 3.6.1,  PyPy 7.1.1-beta0
      # command to install dependencies
      install:
        - pip install pytest
      # command to run tests
      script:
        - cd tests
        - pytest
    - language: php
      php:
        - 7.4
      script: php -r 'echo "Hello World!\n";'
        
